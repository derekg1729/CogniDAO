{
  "type": "Task",
  "status": "todo",
  "project": "../project-CogniMemorySystem-POC.json",
  "name": "Enhance CogniStructuredMemoryAdapter SaveContext Metadata and Tagging",
  "description": "Expand the save_context method in CogniStructuredMemoryAdapter to automatically enrich saved MemoryBlocks with more metadata and dynamic tagging based on inputs/outputs. This will improve future semantic search, graph building, and analytics.",
  "phase": "⚙️ Phase 3: Agent + Tool Wiring (Structured Memory)",
  "implementation_details": {
    "target_file": "experiments/src/memory_system/langchain_adapter.py",
    "depends_on": [
      "task-3.2-implement-cogni-structured-memory-adapter.json"
    ]
  },
  "action_items": [
    "[ ] In save_context, extract important information from inputs and outputs (e.g., session ID if available, keywords).",
    "[ ] Auto-generate additional dynamic tags (e.g., `session:<session_id>`, `keyword:<top_keywords>` if extractable).",
    "[ ] Expand metadata dictionary to include more fields, such as:",
    "[ ]   - Model name/version (if available from config)",
    "[ ]   - Token counts (if tracked, optional)",
    "[ ]   - Latency / time-to-response (if available, optional)",
    "[ ]   - Any other custom interaction attributes relevant to analysis.",
    "[ ] Ensure backward compatibility: If no session ID or keywords are available, default to just the fixed tags.",
    "[ ] Document these metadata extensions clearly in the docstring for save_context."
  ],
  "test_criteria": [
    "[ ] Unit test save_context to verify that dynamic tags and metadata fields are correctly generated when input provides necessary info.",
    "[ ] Unit test fallback behavior when dynamic fields are not available (no crash, uses fixed tags only)."
  ],
  "success_criteria": [
    "[ ] MemoryBlocks saved by save_context automatically have richer metadata for downstream semantic retrieval and graph analytics.",
    "[ ] Saved blocks have dynamic session tagging when available (improving future session-based queries and visualization)."
  ],
  "current_status": "Task definition created after Phase 3.2 code review."
}
